<%= form_for(@twitter_accessor) do |f| %>
  <% if @twitter_accessor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@twitter_accessor.errors.count, "error") %> prohibited this twitter_accessor from being saved:</h2>

      <ul>
      <% @twitter_accessor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
